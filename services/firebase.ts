
// Firebase has been replaced by a local SQLite database on the Express server.
// This file is kept for compatibility with existing imports but exports null/empty objects.

export const db = null;
export const functions = null;
export const auth = null;
